SELECT u.user_id, u.username, COUNT(l.link_id) as link_count
FROM users u
LEFT JOIN links l ON u.user_id = l.user_id
GROUP BY u.user_id, u.username
ORDER BY link_count DESC, u.registration_date ASC
LIMIT 10;
